# AGY 账号代码复核与验证记录

执行进度（非计划）。原执行依据仍为 `docs/plan/DevFlow-AGY账号自动切换与额度管理开发计划-20260920.md` v3.1，AC-R、AC-D、AC-U/I/L/E/H 编号及需求不变。本记录不代替原计划，也不表示功能人工验收通过。

## 复核结论和已修复问题

原实现不宜直接提交：账号控制器未接入真实执行链，凭据锁随单次 helper 退出失效，进程退出使用假成功，额度探测存在模拟回退，账号事务、页面和独立服务有多处缺失。本次按认证、核心事务、API/UI/安装、工作流/进程四个范围并行修复并交叉只读复核。

| 对应任务 | 本次修复 |
| --- | --- |
| AC-D01/05/06/17 | 严格合同、无默认值的局部更新及深层合并、未知额度处理、白名单与模型池约束、持久操作及去重、CAS、候选串行核验、操作期间拒绝账号修改、显式失败不改选、取消屏障、备份/回读/回滚、持久消费者交付 |
| AC-D02/03/04 | 长驻认证 helper、当前 SID 固定凭据域锁、完整凭据加密包、原子保存/ACL/单调 revision、备份失败不清空、读回核验、管道失联与旧实例回调隔离、录入能力指纹、严格解析及能力门禁；移除模拟探测回退 |
| AC-D07/08/09/10 | Profile 与 legacy 执行接线、启动前许可 CAS、真实 PID/Job 确认、当前结构化错误、账号中断不计代码修复、持久检查点、原角色恢复与版本校验、人工停止优先 |
| AC-D10/16 | 关闭 admission 后等待探测/进程/操作退出再释放域锁；未取得能力的服务安全关闭；accounts/full 共用配置、SQLite、controller lock及模式协商 |
| AC-D11/12 | 只读本地额度展示、模型配置、手动自动选号/指定目标、操作进度/取消、补测/重认证/停用/维护；修复HTTP错误被当成功和敏感引用返回 |
| AC-D13 | helper与独立入口构建/分发校验、保留用户配置的升级、稳定快捷入口；Go Host挂起创建并绑定命名Job后才运行，真实doctor/job-status及整树退出确认 |

## 验证范围

- TypeScript类型检查、后端编译、Vite前端构建通过。
- 单元/集成测试按单文件、单worker分别运行；认证、事务、API/UI、安装、工作流、进程、历史错误及基础设施归因均有定向覆盖，详见本次提交对应测试文件。
- 独立账号页面浏览器用例通过，使用14839独立端口、临时SQLite和模拟账号。
- Windows认证helper编译及11项隔离Go用例通过；测试注入内存凭据/锁/加密边界，不访问真实Credential Manager、DPAPI或生产域锁。
- Go ProcessHost完整隔离目标通过，覆盖挂起/绑定、Job计数、重复ID、并发终止、子孙进程、EOF/stop/timeout和Kill-on-close；只操作测试自建进程。
- 没有实机账号录入、A→B→A切换、真实模型请求、授权刷新或真实额度采集；没有发布、推送或重启用户现有服务。

## 未完成项与明确限制

1. **AC-D00/D03/D04仍受外部能力阻塞。** 未取得官方CLI真实身份、双窗口/重置/模型池字段和交互登录命令合同。生产probe及登录适配保持能力未验证；合成fixture通过不允许启用真实切号，不得接入未经批准的私有额度端点补缺。
2. **AC-D08/D09仅完成保守恢复接线。** 当前保留已有根会话和能够识别的子记录；未实现经实机证明的新根接替、全部子Agent原生恢复及临时工作区完整保全。原会话不可用时暂停。UI接替选项标为待验证、不可选；不丢弃已有配置意图。
3. **跨运行白名单没有共同候选时严格等待。** 原计划的FIFO分批接替尚未实现；不能绕过白名单，也不能称相关条目完成。
4. **不确定重启现场不自动继续业务。** 核心只对已知凭据引用做日志对账/回滚或交付重试；工作流状态、版本或配置已经变化时保留人工恢复，避免旧回调覆盖新意图。
5. **AC-L01–06、AC-H01–08未完成，AC-D15不能标全量交付完成。** 需要先补齐真实能力合同，再按原计划完成缺失实现和人工验收。仅当前已具备确定接口的代码路径经过上述隔离验证。

## 提交边界

包含AGY功能源码、必要公共接线、相关测试、构建安装和本记录；保留原计划正文，仅更正末尾执行进度。排除已有暂存 `.tmp-wf-query.cjs`、其他未提交设计文档、依赖目录、构建产物和临时测试数据。凭据和值不进入Git。
