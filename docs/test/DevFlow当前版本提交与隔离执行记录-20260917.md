# DevFlow 当前版本提交与隔离执行记录

日期：2026-09-17
主工作区：C:/Code/system-handle
提交前基线：9f0869117f45ec78dc09048a57d0e2ec356b21a6（main）

本文件记录用户要求提交当前开发版本、将剩余工作交独立 worktree 执行的事实。它不是替代实施计划；后续继续完整遵循 docs/plan/DevFlow剩余修复验收与交付执行计划-20260916.md 及其中引用的正式方案。

## 提交性质与验证范围

- 用户已明确授权提交当前代码。该提交是当前开发进度快照，不是八工具真实模型认证或全部阶段验收完成的声明。
- 本轮重新执行 pnpm run typecheck，退出码 0；没有重新构建主工作区、重启应用或对生产数据运行测试。
- 既有 h03-all.json 记录 270 个用例通过；e2e-report-h02.json 记录 25 个浏览器用例通过。这些是对应当时源码的历史结果，后续变更仍需在隔离 worktree 复验。
- codex-agy-flow-verification 的 execution-spec.json 和 runs.json 均显示两个 Profile 的 executableRef 为 Node，prefixArgs 指向 tests/fixtures/native-cli.mjs；tests/live/codex-agy-full-lifecycle.ts 102—119 行也明确注入该替身。
- 29.10 秒运行证明合成仓库、受控 CLI 子进程、调度状态、证据门禁和 Git 正常路径可以闭环；它没有实际调用两种模型执行规划/开发/审查。真实客户端探针只证明可执行入口和版本。
- summary.json 实际列出 9 项 assertions，全部通过；用户转述中的“10/10”与原文件不符。原始证据不覆盖改写，范围说明见证据目录 verification-scope.json。
- 人工批准与确认在该自动测试中由驱动模拟领域调用，不是用户对真实 CRM 功能的验收。
- compatibility.json 的 live_workflow_verified=false 保持不变。不能用 profile/model 名称或模拟程序输出证明真实模型被调用。

## 当前已知剩余项

独立 worktree 必须先核对并处理：真实 Codex + agy 当前流程认证、生产 LocalRuntime 冲突修复入口缺失、冲突 Run 未绑定 Profile、冲突收据/停止恢复保护、工作流版本与 ExecutionSpec 修订版本混用，以及工具选择保存时默认模型配置问题。冲突用例当前替换了运行时 resolveMergeConflict，不能证明生产调用已接通。

H04 认证脚本存在直接填写 verified 和 true 的逻辑；H05 卸载测试直接正则修改配置而未调用生产卸载入口。按原计划纠正验证范围并补真实入口用例，不得依据旧 PASSED 标签跳过。

H05 未补齐的完整组件安装/升级/卸载、H06 性能基准、H07 Skill/文档一致性、H08 实机矩阵、H09 LF/V 映射、H10 最终冻结回归和 H11 自查/独立复核仍按原计划继续。外部机器/账号缺失如实记 blocked，不能把代码缺失登记为环境阻断。

## 主应用保护边界

提交前实际监听 127.0.0.1:4810 的 Node PID 为 18976，启动时间 2026-09-17 09:57:42.640665 +08:00，入口为 C:/Code/system-handle/dist/apps/api/src/main.js；/api/health 返回 ok=true、service=devflow、version=0.2.0。PID 仅为记录时事实，不用于后续盲目终止。

剩余执行必须遵守：

1. 只在独立 worktree 修改、构建和测试；不共享主工作区 dist、.devflow、数据库或运行配置。
2. 不复制主 devflow.yaml、.env、用户数据库和 token。测试使用独立数据目录及回环空闲端口；不得访问生产写入接口。
3. pnpm 依赖使用共享包存储，在 worktree 建立自己的 node_modules；禁止通过指向主 node_modules 的目录链接操作主依赖。
4. 长集成、浏览器、安装、性能和真实模型验证串行，测试 worker 为 1；不得同时启动多个资源密集任务。
5. 只停止本任务启动并核验身份的进程，不按旧 PID、进程名称或端口批量终止。
6. 不自动合回 main、不重启/部署当前服务、不推送或发布。worktree 修复验证完成后保留独立提交供复核。
7. 本机全局 Skill 同步会影响用户的新会话；先在隔离客户端 HOME 校验。实际全局同步及主服务部署留到用户明确安排的切换时进行。
