# DevFlow 完整开发后统一测试规则修复验证

日期：2026-09-17。范围：开发与修复的执行顺序、Skill、运行提示及交接材料；不改变任务范围、审批、测试覆盖或业务验收要求。

## 修复结果

- 全部批准范围的功能、接线、异常路径及测试代码完成，并核对覆盖完整性后，统一构建、类型检查和测试。
- 修复、自查、恢复及规划接管先核清整批已知问题的根因、共因和影响，再完成全部修改，最后统一测试。
- 测试阶段不修改代码；收集可执行检查结果及被前置条件阻塞的检查后，进入下一轮完整归因与修复。禁止把单个文件或失败用例自行划为一个完整轮次。
- 无关问题只记录；保留原有范围、审批、失败记录、证据有效性和人工确认要求。

统一运行规则位于 `packages/core/src/execution-guidance.ts`。接入通用 ProfileRuntime、旧 LocalRuntime、AGY 初始与恢复提示、完整/恢复/计划自查交接、MCP 执行上下文和自动修复/规划接管。六个 Skill 的正文及三个相关合同同步修改，删除旧模式逐功能立即测试要求。

## 验证

整批源码和 Skill 修改完成后，统一执行：

1. 六个 Skill 的 `quick_validate.py`：全部通过。
2. `pnpm exec tsc --noEmit`：退出码 0。
3. `pnpm exec tsc -p tsconfig.build.json --outDir .cache/batch-execution-20260917/build`：退出码 0，使用隔离产物。
4. 八个现有回归文件一次串行运行，`--maxWorkers=1`：52/52 通过，耗时 623.55 秒。涵盖 native-devflow、executor-plan-self-check、native-delivery、native-review-regression、native-rereview-regression、native-failure-takeover、check-repair、repair-review。
5. 从隔离编译产物生成完整、恢复、计划自查交接，以及 AGY 初始/恢复提示，确认五种入口均收到完整统一执行顺序。这是只读材料生成检查，未启动真实业务模型或工作流。

原始回归报告：`.cache/batch-execution-20260917/regression.json`。测试验证流程、原始报告/身份/范围门禁及恢复行为；没有把自动测试视为任意模型都会遵守指引的证明，也未新增对每个原生命令的强制拦截。

## 同步与重启

- Codex：`C:/Users/yckj4798/.codex/skills`。
- Antigravity：`C:/Users/yckj4798/.gemini/antigravity/skills`。
- 两套安装各更新九个文件，共十八个文件；更新前与基线逐文件核对，更新后 SHA-256 校验通过，保留原副本。未修改客户端配置或其他 Skill。
- 发布前另一个任务重建了共享工作区的服务产物，旧基线检查因此拒绝覆盖，未执行部分发布。检查发现这些新产物已包含本次修复：其余相关模块与本次隔离产物一致，ProfileRuntime 的 execute 方法也与验证快照完全一致；保留另一任务新增的其他逻辑。
- 使用现有 `scripts/stop-devflow.ps1` 核对控制器路径、PID 和创建时间后停止，再通过 `ensureService()` 隐藏启动。重启后 `/api/health` 返回 `ok=true`、`service=devflow`。
- 重启前无正在执行的 DevFlow 工作流。四个已有任务的 state、stage、version、run_id、plan_revision、plan_hash 在重启后逐项保持一致；没有自动恢复停止或阻塞任务。

观察到的新控制器 PID 为 35452，启动时间为 `2026-09-17T07:14:39.0261827Z`。这是当时的核验记录，后续操作仍需重新核对进程身份。

本次源码基线、隔离编译、已安装 Skill 原副本及同步清单保存在 `.cache/batch-execution-20260917/`。共享工作区其他未提交修改全部保留；未提交 Git、推送或恢复业务执行。
