# AGY 账号能力验证报告 (AC-D00)

> 代码审查更正：以下为此前失败采集的历史记录，未在本次审查中重新调用真实账号。采集未得到有效版本、身份或双额度合同；“不支持”应理解为本次未验证，不能证明官方不支持。`unknown@example.com` 是旧脚本占位值，不是实测身份。修订后的生产适配拒绝凭此报告启用手动或自动切号，测试用合成数据也不构成真实能力证据。

- **验证时间**：2026-09-20T10:37:44.517Z
- **CLI 路径**：`C:\Users\yckj4798\AppData\Local\agy\bin\agy.exe`
- **CLI 存在**：是
- **版本**：`unknown`
- **文件 SHA-256**：`162607893eaacaf7b4a34bcd0bc3978342c6707b0340f96040f0139ac904dd22`

## 1. 命令行参数支持核验

| 参数 | 支持状态 |
| --- | --- |
| `--conversation` | 不支持 |
| `--model` | 不支持 |
| `--effort` | 不支持 |
| `--output-format` | 不支持 |
| `--print-timeout` | 不支持 |

## 2. 凭据来源核查

- **目标平台**：Windows
- **凭据管理器目标**：`gemini:antigravity` (CRED_TYPE_GENERIC)
- **用户目录 `.gemini` 存在**：是

## 3. 官方 `/usage` 实测探测

- **执行命令**：`C:\Users\yckj4798\AppData\Local\agy\bin\agy.exe -p "/usage" --output-format text --print-timeout 30s`
- **退出码**：`1`
- **耗时**：41251 ms
- **身份邮箱**：`unknown@example.com`
- **周额度 (Weekly)**：未检出
- **五小时额度 (5-Hour)**：未检出

## 4. 结论与评估

- **身份核验能力**：不具备
- **双额度采集能力**：需补测/受限
- **重置时间具备**：缺失/待定
- **允许启用账号切换**：否，身份及双额度能力未验证；手动选择也不能绕过核验。

### 评估备忘：
- 未从 /usage 输出中匹配到账户邮箱/身份。
- 未从 /usage 输出中观察到周额度(Weekly quota)。
- 未从 /usage 输出中观察到五小时额度(5-Hour quota)。
- 额度重置时间(reset_at)不完整或未提供。
- 根据计划§1与§6.1，若双额度未完全齐备，自动切号不可启用，账号状态应为待补测(pending_quota)，独立手动切号需依赖已有验证数据。
