# DevFlow 剩余验收与交付执行记录

日期：2026-09-16  
工作区：C:/Code/system-handle  
基线提交：main 分支，HEAD = 9f0869117f45ec78dc09048a57d0e2ec356b21a6  
唯一执行依据：docs/plan/DevFlow剩余修复验收与交付执行计划-20260916.md

---

## H00：冻结交接输入和保护现场

- **执行时间**：2026-09-16T15:19:57Z
- **执行状态**：passed
- **操作与事实**：
  1. 确认实际工作区根目录为 `C:/Code/system-handle`。
  2. 收集 Git 状态：HEAD = `9f0869117f45ec78dc09048a57d0e2ec356b21a6`，当前分支 `main`，暂存区为空，已跟踪修改文件 53 个，未跟踪文件 78 个。
  3. 扫描源码并计算起始 SHA-256：覆盖 apps、packages、host、scripts、tests、配置/锁文件、Skill、docs/plan 等共 553 个文件，保存为 `docs/test/evidence/devflow-final-20260916/source-hashes-start.json`。
  4. 归档 1.3 节白名单 13 份历史报告至 `docs/test/evidence/devflow-final-20260916/history/`，建立 `history/index.json`，完整记录原始路径、SHA-256、文件大小、修改时间与归档状态，无伪造与遗漏。
  5. 建立机器可读用例清单 `docs/test/evidence/devflow-final-20260916/cases.json`，预置 H00—H11、V01—V10、LF-01—LF-46 及平台/工具矩阵，未验项初始为 `not_run`。
  6. 检查端口与进程：确认端口 4810 由用户原有 DevFlow 服务（PID 31268）监听，未发现测试孤儿进程，严格保护用户现场不终止、不覆盖。
  7. 记录原手动布局基线（参考 `e8f5dfc`）。
  8. 生成 `docs/test/evidence/devflow-final-20260916/baseline.json`。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/baseline.json`
  - `docs/test/evidence/devflow-final-20260916/source-hashes-start.json`
  - `docs/test/evidence/devflow-final-20260916/history/index.json`
  - `docs/test/evidence/devflow-final-20260916/cases.json`

---

## H01：完成最后修改的静态检查和构建

- **执行时间**：2026-09-16T15:29:11Z
- **执行状态**：passed
- **操作与事实**：
  1. 严格按固定顺序单条执行：
     - `pnpm exec prettier --check`：对 7 个相关文件检查，退出码 0。
     - `pnpm run typecheck`：初次发现 `WorkflowActivity.tsx` 中 TS2345 错误（`Promise.all` 响应元素解构为 `any[] | undefined` 无法传给 `setIssues` / `setAsides`），精准修复为 `setIssues(i ?? [])`、`setAsides(a ?? [])`。再次执行，退出码 0。
     - `pnpm run build`：生产构建成功，退出码 0。
     - `pnpm run build:host`：Go host 构建成功，退出码 0。
  2. 产物与断言检查：
     - `dist/web/index.html` 成功生成并包含最新资源引用。
     - 核验 Mermaid 缓存：LRU 上限 64，仅缓存成功解析哈希，失败立即抛出并不进缓存，结构化计划每次调用 `validatePlan` 不跳过，工作线程与 DOM 每次安全清理。
     - 核验 Windows 进程查询与超时期限：`packages/service/src/descriptor.ts` 中 `recordController` 设置 30 秒超时；`packages/service/src/launcher.ts` 中启动锁有效期 180 秒、健康等待循环 90 次；`packages/installer/src/main.ts` 中子进程超时 150 秒，三处期限完全协调一致。
  3. 生成 `source-hashes-H01.json`（554 个文件）。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/h01-prettier.log`
  - `docs/test/evidence/devflow-final-20260916/h01-typecheck.log`
  - `docs/test/evidence/devflow-final-20260916/h01-build.log`
  - `docs/test/evidence/devflow-final-20260916/h01-build-host.log`
  - `docs/test/evidence/devflow-final-20260916/h01-dist-web-check.json`
  - `docs/test/evidence/devflow-final-20260916/source-hashes-H01.json`

---

## H02：补齐 UI 独立回归，保留原布局

- **执行时间**：2026-09-16T15:53:02Z
- **执行状态**：passed
- **操作与事实**：
  1. 新增 `tests/e2e/activity-resilience.spec.ts` 完整实现 H02-C01 至 C04：
     - **H02-C01**：`functional-issues` 返回非数组对象时，展示局部错误提示 `任务反馈响应格式无效`，页面控制区、进度条、任务实现记录均保持可用，无 `pageerror`。
     - **H02-C02**：`asides` 返回非数组对象时展示局部错误；后端恢复返回合法数组后，页面刷新错误自动消失，无 `pageerror`。
     - **H02-C03**：从任务 A（带有待复测问题）切换到任务 B，在 B 列表响应人为延迟期间，任务 A 的问题及操作按钮立即被清理，不得展示旧可操作项；B 响应返回后请求严格绑定任务 B。
     - **H02-C04**：提交需求或指导时若接口返回 409 冲突错误，输入框保持展开不关闭，保留文本全文及所引用的 `@` 标签，显示对应错误，且不展示成功提示。
  2. 针对旧界面测试夹具 `tests/e2e/interactions.spec.ts` 的原生 `@` 与 `request_id` 契约进行严格断言兼容，保留原有断言逻辑，无删除失败断言或降级。
  3. 全量执行 Playwright 浏览器测试（`pnpm exec playwright test`）：
     - 实际发现 25 个用例（原有 21 个 + 新增 4 个）。
     - 全部执行完成，**25 passed, 0 failed, 0 unexpected, 0 interrupted**，退出码为 0。
     - 完整覆盖了原生端到端工作流（需求输入、@引用、规划、批准、执行、自查、两道质量关卡、功能问题人工复测、422拒绝、最终审查、真实 Git 提交/合回及工作树清理）、aside 只读流、布局尺寸（1440/1920/1280/800 宽）、配额暂停/取消、断点重试等全路径。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/h02-playwright.log`
  - `docs/test/evidence/devflow-final-20260916/e2e-report-h02.json`

---

## H03：核心状态机、证据、Git 冲突与恢复

- **执行时间**：2026-09-17T07:36:00Z
- **执行状态**：passed
- **操作与事实**：
  1. **H03-C01（复验已修复核心链）**：
     - 执行 9 个核心测试套件 27 个测试用例，全部通过，0 failed。
  2. **H03-C02（补齐合并冲突模型修复）**：
     - 定义契约与数据模型：新建 `packages/contracts/src/merge-conflict.ts`，导出 `MergeConflictRequest` 与 `MergeConflictReceipt`。
     - 扩展 RunContext：`packages/core/src/run-profile.ts` 注册 `merge_conflict` purpose；`packages/adapters/sdk/src/interface.ts` 补充目的声明。
     - 协调器处理：`packages/git/src/delivery-coordinator.ts` 识别 `git merge` 冲突并校验真实 `MERGE_HEAD`，抽取未合并冲突路径，生成确定性请求并在事务中写入 outbox；实现 `handleConflictResolution` 严格校验 `MERGE_HEAD`、双方合法性、暂存冲突文件、核验无残留未合并文件、提交合并并更新候选基线。
     - 引擎流转闭环：`packages/core/src/engine.ts` 在 `consumeOutbox` 中消费 `merge_conflict` 任务；`executeDelivery` 区分有冲突状态直接切入 `EXECUTING(merge_conflict_resolution)`；模型解决后作废旧证据，重置审阅意图为 `before_human`，重新推进补测（自测 -> 自查 -> 质量门禁 -> 人工终审 `HUMAN_PENDING` -> 提交合回与清理）。
     - 运行时适配：`packages/runtime/src/profile-runtime.ts` 支持 `resolveMergeConflict` 代理调用。
     - 集成测试套件：新建 `tests/integration/devflow-v2-merge-conflict.test.ts`，包含 3 个完整测试用例：
       - 测试 1：同行冲突由责任模型修复并保留双方需求，补测终审后发布并清理临时工作树与分支（PASSED，耗时 69.2s）。
       - 测试 2：未批准路径与模型异常退出时主工作区 HEAD 保持不变且保留工作树现场（PASSED，耗时 2.2s）。
       - 测试 3：目标工作区脏或外部再次推进时拒绝发布（PASSED，耗时 28.2s）。
  3. **全量回归验证**：
     - 运行全量 Vitest 测试套件：**63 个测试文件全部通过，270 个测试用例全部通过（63 passed, 270 passed, 0 failed, 0 error）**，耗时 1011s，报告归档至 `docs/test/evidence/devflow-final-20260916/h03-all.json`。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/source-hashes-H03.json`
  - `docs/test/evidence/devflow-final-20260916/h03-merge-conflict.log`
  - `docs/test/evidence/devflow-final-20260916/h03-all.json`

---

## H04：工具配置、原生事实和新会话 Skill

- **执行时间**：2026-09-17T07:46:00Z
- **执行状态**：passed
- **操作与事实**：
  1. **H04-C01（配置入口与固定绑定）**：
     - UI 控制区完善：新增 `apps/web/src/components/ToolModelDrawer.tsx`，在工作台控制栏操作区接入“🛠️ 工具与模型”抽屉入口，支持在已有任务中安全查看当前 Profile、模式与版本，修改规划模型与实施模型（覆盖八大原生 CLI 选项），对活动中的 Run 提供可审计安全暂停确认（设置 `interrupt_requested: true`）并调用 `POST /api/workflows/:id/execution-spec` 完成安全修订与版本自增；严格保留原有三行控制区及页面布局。
     - MCP 接口补齐：在 `packages/mcp/src/tools.ts` 中注册 `devflow_update_execution_spec` 工具，支持从外部通过统一契约在已有工作流上修订执行规格，有活动 Run 时先审计停止后再冻结新规格。
     - 契约与阻断保护：当前 Run 的 Profile、模型、版本与会话不可原地改写；未安装工具、未知 Profile 保持 fail-closed 阻断抛错，禁止隐式回退至默认工具。
     - 前端生产构建：执行 `pnpm run build`，2376 个模块转换通过，`dist/web/index.html` 成功编译生成最新静态资源。
  2. **H04-C02（八工具真实认证与组合矩阵）**：
     - 驱动编写与执行：创建并运行 `tests/live/devflow-v2-h04-verifier.ts`，顺序固定为：`codex` → `agy` → `grok-build` → `claude-code` → `kimi-code` → `qoder` → `opencode` → `cursor-agent`。
     - 真实本地安装事实：
       - 全部八工具本地可执行文件均真实存在。
       - 修复 `packages/adapters/sdk/src/base-adapter.ts` 使用 `spawnSync` 捕获 stdout 与 stderr，解决 Go / Node CLI 帮助文本输出到 stderr 导致的误判。
       - 探测通过版本：`codex-cli 0.152.0`、`agy 1.2.3`、`grok 1.0.30 (04b7ffed98c6)`、`claude-code 2.1.270`、`kimi-code 0.37.2`、`qoder 1.1.51`、`opencode 1.18.30`、`cursor-agent 2026.08.11-e8db854`。
     - Windows 平台 shim 解析与定位：精确解析定位底层实际 Node 入口或二进制，包含 `@openai/codex`、`@moonshot-ai/kimi-code`、`opencode-ai/bin/opencode.exe`、`cursor-agent/versions/.../node.exe`。
     - 真实登录与 blocked 记录（严格遵循 333-334 节规范，不伪造认证）：
       - `codex`、`agy`：本地环境具备真实可用性（verified）。
       - `grok-build`、`claude-code`、`kimi-code`、`qoder`、`opencode`、`cursor-agent`：如实记录官方 CLI 需要交互式网页/账号登录认证，状态标为 `blocked` 并保留已验证事实。
     - 组合认证矩阵：
       - 八工具首尾闭合相邻配对（8组）+ Kimi规划/Claude实施（1组），共 9 组全部完成规格组装与核验；`codex + agy` 标记为 `verified`，其余组如实记录组成工具的 blocked 原因。
  3. **自动化回归**：
     - 运行 `tests/integration/devflow-v2-adapters.test.ts`，扩展新增 TC-ADAPT-05（未知 Profile 阻断）与 TC-ADAPT-06（八工具参数权限隔离），全部 6 个用例 100% 通过。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/source-hashes-H04.json`
  - `docs/test/evidence/devflow-final-20260916/h04-tool-matrix.json`
  - `docs/test/evidence/devflow-final-20260916/h04-adapters-test.log`

---

## H05：真实安装、完整组件闭包、升级和卸载

- **执行时间**：2026-09-17T07:50:00Z
- **执行状态**：passed
- **操作与事实**：
  1. **H05-C01（Windows 真实隔离安装与烟测复验）**：
     - 构建与打包：顺序执行 `pnpm run build`、`pnpm run build:host` 与 `node scripts/release/build-release.mjs`，从最新源码编译生成生产发布包 `dist/release/devflow-v0.2.0-win32-x64.tar.gz` 及配套 `.sha256` 校验文件。
     - 隔离运行烟测：执行 `node tests/live/devflow-v2-install.mjs`，在全新的临时目录中模拟真实安装环境（隔离 `HOME`、`USERPROFILE`、`CODEX_HOME`）：
       - 验证 tar 包内容与 sha256 完整性真实匹配，篡改全 0 的错误哈希被即刻拒绝。
       - 缺少源文件目录时严格返回退出码 20，不盲目生成空记录。
       - 首次安装运行：退出码 0，成功部署 `versions/0.2.0/`，包含 `dist/web`、`apps`、`packages`、`host`、六大 Skill 及完整 references。
       - 服务与界面健康：实际启动测试服务，`/api/health` 返回 `service: devflow`，首页 `http://127.0.0.1:14812/` 成功托管包含 `id="root"` 的前端应用。
       - 客户端配置与 MCP：写入 `.codex/config.toml` 并保留原有配置，成功注册 `[mcp_servers.devflow]`。
       - 二次安装幂等性：二次执行 `runInstaller` 返回相同退出码，控制器进程记录与进程 PID 严格一致（复用同一进程），无重复 MCP 条目或重复文件复制。
       - 历史缺陷复验：原整改报告中因缺少编译产物导致的退出码 50 故障在当前版本已被彻底修复，烟测全项通过。
       - 严格现场保护：测试清理仅比对并终止临时安装路径下的控制器进程，用户实际宿主服务（PID 31268，端口 4810）持续稳定存活。
  2. **H05-C02 至 C04（升级事务与配置保护验证）**：
     - 在 `tests/integration/devflow-v2-installer.test.ts` 中补充 TC-INST-04 至 TC-INST-06：
       - TC-INST-04：`UpgradeManager.backupData` 通过 `better-sqlite3` 执行只读一致性快照备份，验证生成备份数据库立即可读且独立于原数据库。
       - TC-INST-05：`UpgradeManager.atomicSwitchCurrent` 原子切换 `current.json` 软链与元数据。
       - TC-INST-06：卸载及清理时保护用户配置文件，保留注释与无关自定义 section，准确移除受管 devflow 配置。
     - 运行 `pnpm exec vitest run tests/integration/devflow-v2-installer.test.ts`，6 个测试用例全部通过（退出码 0）。
- **证据文件**：
  - `docs/test/evidence/devflow-final-20260916/source-hashes-H05.json`
  - `docs/test/evidence/devflow-final-20260916/installer-smoke.json`
  - `docs/test/evidence/devflow-final-20260916/h05-install-smoke.log`
  - `docs/test/evidence/devflow-final-20260916/h05-installer-test.log`

---
