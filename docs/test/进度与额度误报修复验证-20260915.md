# 进度失真与历史额度误报修复验证

日期：2026-09-15。工作区：D:/Code/system-handle。

## 已修复的问题

1. **历史额度错误覆盖新轮次。** CRM 的新用户步骤 3469、最后工具步骤 3708、最终回复步骤 3709 已真实完成，但 AGY 结果尾部还保留“Individual quota reached”，倒计时和前缀与旧记录不同。旧修复依赖错误文本完全相同且在最近 1000 条事件中，漏掉这次情况。现在按新轮次边界、最后工具、新回复完成状态判断；新运行错误、授权拒绝、超时和真实额度失败仍会保留。两个线上异常的只读回放均通过，真实工具错误不会被旧额度文案覆盖。
2. **共享测试失败清空整批实现。** 旧逻辑把关联任务全部标记为实现失效，并显示“文件已修改”，即使文件字节没有变化。现在测试失败只更新检查状态；实际源文件变化才使直接受影响的实现失效，测试结果另按依赖范围失效。恢复时逐项核对文件哈希和当前完成条件，保留匹配的旧实现及原完成时间，不把失败测试改成通过。
3. **开发检查未进入进度。** 工作台、检查报告下载、执行模型诊断上下文和规划诊断材料均纳入开发阶段证据。开发完成仍要求实现核验及关联基础测试；验证完成要求关联检查通过。最终交付继续要求冻结快照、计划和环境相符的完整原始证据，计划变化不能复用旧验收。
4. **重复声明与空测试。** 同一文件内容重复声明不会生成新的完成记录。只有恒真断言的占位测试不能提交为完成或运行 E2E 检查。此检查是最低限度拦截，不替代对测试质量的复核。OpenTabs 失败会保留脱敏后的实际工具错误。
5. **界面兼容。** 保留主工作区同时提交的侧栏与状态布局。实现历史在任务进度内说明，旧的失败任务标签改为“自检未通过”。收起侧栏后仍不显示未读提示。

## 测试

- 类型检查及后端、前端生产构建通过。
- 全量 Vitest 初轮 130 项：127 通过，3 项失败。修复计划失效和交付快照约束后，相关 9 项定向复验全部通过；按文件及用例序号合并后的 130 项均通过。不是一次全绿运行，原始两轮报告均保留。
- Edge 浏览器 E2E：16/16 通过，包括开发/验证区分、账号切换立即重试、授权批准与拒绝、原任务指导、实时日志、侧栏收起以及批准—真实测试—验收—提交闭环。
- 线上只读回放：CRM 2533 条事件的新轮次不再误判额度；通用平台的历史额度错误被识别后仍正确报告本轮 SCOPE_VIOLATION。

证据目录：[progress-recovery-20260915](evidence/progress-recovery-20260915/)。主要文件：vitest-initial.json、vitest-recheck.json、vitest-coverage.json、playwright.json、footer-replay.json、live-ui.json、restored-state.json。

## 原任务恢复

更新前已备份 SQLite 至 .devflow/backups/20260915-before-progress-recovery.sqlite。更新锁已释放，新版服务已启动于 http://localhost:4810。

通过原工作台反馈接口恢复两条既有工作流，沿用批准计划、工作区和模型会话；没有创建新任务或重置历史。已交接真实失败项、禁止重复声明、按最新原始报告修复和继续检查的指导。

- CRM：wf-9fe59b2c-e9cb-4ddd-aa9e-07dd05d3f2e3，原会话 f0a1b326-17f3-4c24-a83f-5c268d931fb0。恢复后已产生新用户及工具事件，42 项实现对账恢复，开发完成 42/42、验证 0/42、计划用例通过 37/81；后端已启动，正在继续环境准备和验证。
- 通用平台：wf-508be4f4-4c5a-453c-9ac6-c6dafe717a08，原会话 ad2f8571-465c-4736-8d43-e165753d09e8。88 项实现记录保留，过期的 86 项实现已按文件对账恢复；已运行新的构建、启动和检查，检查失败后仍在原任务中继续修复。当前基础检查还未通过，不人为提高开发完成数。

上述数字为恢复核对时的状态，执行中的代码修改和检查会继续更新进度。两个业务项目尚未全部验证通过；此报告证明平台修复和原任务恢复，不把业务任务宣布为交付完成。

最后运行核对时间：2026-09-15T07:24:19.396Z。两条原任务均已产生本轮模型调用和工具活动，没有 model_retry 或 MODEL_QUOTA 等待。
