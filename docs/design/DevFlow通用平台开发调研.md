# DevFlow 通用平台开发调研

修订日期：2026-09-16。此文件是 v2.0 的现状索引，不是执行合同。

## 已核对的现状

- 项目实际位于 C:/Code/system-handle；旧 D 盘目录不可用。
- 主工作区已有原生执行改造、pnpm 迁移与手动界面布局，包含未提交修改。
- packageManager 固定 pnpm@11.7.0，Node 要求至少 22.23.2。
- 现有页面保留 CentralWorkspace、workspace-columns、ExecutionPanel、TaskInteraction、计划/测试/差异/复核/环境等区域。新版功能应在这些位置加入，不能据旧方案重做布局。
- 原生改造包含执行适配器、宿主事实、交付导入、批次指纹和缓冲组件；第二轮复核揭示的缺口须在当前代码验证，不能以文件存在认定完成。
- 原跨平台任务已按用户指令取消，旧执行工作树及临时分支已移除。未提交实现和任务记录有本地私有备份。
- 调研期间主工作区存在并行修改，特别是 engine.ts/runtime.ts；本次只读这些实现，不覆盖或回退。

## 已确定的新方向

[完整 v2.0 方案](../plan/DevFlow跨平台通用工作流平台实施方案.md)统一定义轻量原生调度、双入口、@ 引用、全过程反馈、隔离临时提问、两道质量关卡和三次接管、人工功能复验、最终 Git 整合清理、八工具及安装/开源范围。

旧“逐工具代理、逐细项任务上报、固定测试函数名、程序重跑测试、交付直接转人工验收”均不再作为新任务设计。

## 证据边界

本次没有启动新的开发工作流，没有运行真实模型开发，也没有更改界面或后端实现。源码基线、质量缺陷修复状态和性能基准须由手动实施的 N00/N01 确认。历史版本命令及帮助探针不能代替新版原生协议、安装和完整工作流认证。
