# AGY 账号整改代码复核意见

日期：2026-09-21。结论：**changes_required**。

审查基线为 `C:\Code\system-handle`、HEAD `004b2ea153a80f94007765e996d936db56d74c1c` 的当前未提交整改，包括 tracked diff、新增源码以及必要上下游。依据为 [原开发计划 v3.1](./DevFlow-AGY账号自动切换与额度管理开发计划-20260920.md) 和 [功能补全与恢复修复计划 v1.0](./DevFlow-AGY账号功能补全与恢复修复计划-20260921.md)，后者 SHA-256 为 `B45B71C854FC805CB3350B212D10318EC17EA3CA92F7E56BF89201C41EF51355`。

主审及三个子 Agent 分别核查认证、账号核心、工作流恢复和 API/界面，合并得到下列 29 项整改意见。本轮只读审查产品代码，未修改源码、重复运行测试、操作真实账号或查验执行者的测试报告。仅新增本文，不提交或推送。

这些是实现遗漏或逻辑错误。官方合同确实缺失时，原计划允许对应能力保持未验证；本审查不要求伪造能力，也不因缺少实机测试材料判失败。对于尚无生产调用者的新函数，下面明确区分“业务仍未接通”和“函数接通前必须修正的问题”。本文是对原计划的代码审查意见，不替代原计划或削减其范围。

## 认证与独立账号操作

### CR01 · P1 · 生产适配器被无条件视为已验证，实际仍使用 fixture parser

位置：[account-service-bootstrap.ts:51](C:/Code/system-handle/apps/api/src/account-service-bootstrap.ts:51)、[account-probe.ts:22](C:/Code/system-handle/packages/adapters/agy/src/account-probe.ts:22)、[quota-parser.ts:120](C:/Code/system-handle/packages/adapters/agy/src/quota-parser.ts:120)。关联 AGF-D00/D01/D03。

组合根对任何存在的 CLI 文件计算摘要后硬编码版本 `1.2.7`，直接创建 `VerifiedUsageAdapter` 和 `login` 命令；并未由已确认的版本合同决定其命令及字段。factory 调用的 parser 自身注明是 fixture 格式，且始终返回 `pools: []`，因此当前生产探测不可能完成双窗口及模型池初始化。新 `evaluateAgyCapabilities()` 没有生产调用者，还会仅按版本字符串、默认 Host 布尔值发布 verified。`probeIdentity()` 另行绕过适配器/指纹及退出码检查，解析同一 `/usage` 文本。

修复：以明确的官方版本/摘要/Host 合同注册命令和解析器，identity、usage、login 统一从已装配合同取得能力；缺失时保持 unverified，并说明具体受阻项。不得把任意文件刚算出的 hash 当作该版本已验证的证据，也不能把当前 fixture parser 改个名字投入生产。完整双窗口及模型池不能取得时继续报告受阻。

### CR02 · P1 · 辅助进程不属于原生 Job，却报告整树已停止

位置：[agy-account-job-runner.ts:189](C:/Code/system-handle/packages/process/src/agy-account-job-runner.ts:189)、[agy-account-job-runner.ts:217](C:/Code/system-handle/packages/process/src/agy-account-job-runner.ts:217)、[account-probe.ts:243](C:/Code/system-handle/packages/adapters/agy/src/account-probe.ts:243)。关联 AGF-D02。

实际登录使用 `cmd/start`；取消仅异步启动 `taskkill`，收到包装进程 close/error 就返回 `fully_stopped: true`。探测仍直接 spawn/kill 父进程。新增辅助 probe 没有生产调用者，lease 参数也未使用。两个 Process Host 未接入本轮要求的可见登录协议。包装进程退出不证明 CLI、callback 或后代已停，后续回滚/换号可能与旧身份活动重叠。

修复：使用用户配置的 C# 或 Go Host，辅助 lease 持久化并在启动前校验；挂起创建、加入拥有的命名 Job、再恢复。取消/管道失联撤销许可并停止 Job，实际活动进程数归零后才能报告 fully_stopped；无法确认时保留屏障和 journal，不恢复或安装另一身份。

### CR03 · P1 · 模型探测可将空输出或矛盾结果缓存为成功

位置：[account-probe.ts:229](C:/Code/system-handle/packages/adapters/agy/src/account-probe.ts:229)。关联 AGF-D01/D09。

`(code === 0 && !hasError) || hasSuccessResult` 会接受空 stdout + exit 0；同一输出既有成功又有错误时，后半条件也能使验证成功。未检查实际 init/model/CWD，结果却缓存 24 小时并用于候选准入。

修复：使用已确认 AGY 协议的当前调用成功终态，要求退出码、初始化身份/模型/CWD 均匹配；缺失、错误、旧事件或矛盾结果不得发布成功。不混入其他 CLI 的 envelope 作为宽松兜底。

### CR04 · P1 · 录入时 identity 和后续 usage 可以属于不同账号

位置：[enrollment.ts:150](C:/Code/system-handle/packages/agy-accounts/src/enrollment.ts:150)。关联 AGF-D01/D03。

前一步核实 email=A 后，后续 usage 的 `observation.email` 未与 A 比较，pools 直接归入 A；请求也未携带足以核验操作/账号/代次的绑定。若返回 B 的观察，会污染 A 的额度。当前内置 parser 返回空 pools 暂时掩盖该分支，接入真实 adapter 后不能沿用此实现。

修复：观察携带 operation、account、credential revision、epoch 和协议来源；入库前重新校验身份及操作仍有效。实际身份不一致时冻结操作，不落入普通 pending 或把结果改名归给预期账号。

### CR05 · P2 · capture 后没有先持久化 pending 账号及操作引用

位置：[enrollment.ts:144](C:/Code/system-handle/packages/agy-accounts/src/enrollment.ts:144)、[enrollment.ts:229](C:/Code/system-handle/packages/agy-accounts/src/enrollment.ts:229)。关联 AGF-D03。

虽然 secret capture 已前移，但之后先 await quota，账号直到最后才入库；期间取消会在 requireContext 抛出，崩溃也会留下未关联账号/操作新引用的 secret，不能按“授权已保存，直接补测”续办。

修复：身份已核实且 capture 成功后，先事务保存 pending 账号和 journal 引用，再执行 quota；完成后单独更新快照及录入状态。取消/重启保留已完成的授权录入事实。

### CR06 · P2 · 未识别的认证格式被升级为 verified

位置：[auth_metadata.go:103](C:/Code/system-handle/host/devflow-auth-host/auth_metadata.go:103)、[enrollment.ts:203](C:/Code/system-handle/packages/agy-accounts/src/enrollment.ts:203)。关联 AGF-D04。

helper 只要在任意 JSON 中发现可解析 expiry 或 refresh_token 字段就标 verified，没有验证编码/格式版本；错误类型也可能被解释为明确没有 refresh。Node 在 helper 未提供元数据时还补 `false` 和 `metadata_status: verified`。

修复：只按已验证格式解析安全字段；未知/错类型保留 null/unrecognized，不推导授权缺失。Node 仅透传可核验元数据，不替 helper 补肯定结论；保存原始加密包不依赖解析成功。

### CR07 · P2 · 持久能力诊断操作只有枚举，没有执行路径

位置：[agy-account.ts:235](C:/Code/system-handle/packages/contracts/src/agy-account.ts:235)、[service.ts:1816](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1816)、[probe-agy-account-capabilities.ts](C:/Code/system-handle/scripts/probe-agy-account-capabilities.ts)。关联 AGF-D00/D02。

`capability_check` 已加入类型，但 service 没有对应分支，落入 unsupported_operation；调查脚本仍直接 execFile，没有经过计划规定的持久诊断操作和辅助 Job 屏障。

修复：保留纯信息阶段，实现受控内部诊断操作，再接真实采集。只豁免待调查能力的 verified 前置条件，不豁免域锁、旧 Job 退出、命令白名单、取消及真实操作授权。

## 账号调度、等待、维护和重启

### CR08 · P1 · FIFO 批次、持久需求及排空没有生产接线

位置：[switch-operation.ts:172](C:/Code/system-handle/packages/agy-accounts/src/switch-operation.ts:172)、[recovery-batch.ts:183](C:/Code/system-handle/packages/agy-accounts/src/recovery-batch.ts:183)、[service.ts:1488](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1488)。关联 AGF-D05。

生产仍把全部占用白名单求交、必需池求并；新增 batch/demand 仓储没有调用者。两个任务分别只允许 B、D 时仍全域等待，提交后也没有 selected/deferred 投递及排空接替。新 availability 函数还会把 reset 已到的实测 0 永久排除，并漏检不存在的五小时窗口，不能仅接上调用线。

修复：Bridge 建稳定 opaque demand；service 按就绪 FIFO 持久规划批次，switch 使用 anchor 候选，只恢复 selected。最后许可释放时规划下一批，存在当前可运行的 deferred 才限制后到准入。统一窗口评估，完整双窗口为前提，reset 到时只获得串行核验资格。

### CR09 · P1 · 长等待仍受旧 deadline 限制，另一分支又会充值预算

位置：[service.ts:1368](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1368)、[service.ts:1312](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1312)、[service.ts:1444](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1444)。关联 AGF-D05/D06。

初始无候选只写 retry_at，保留约 300 秒 deadline；几小时后唤醒先超时。waiting 长期占住 realm.pending_operation_id，维护/补测也受阻。激活后耗尽分支则清空尝试集合并补发完整预算，未保存同一选号轮次的已耗时及一次性 wake key。

修复：持久 demand 与有限 attempt 分离，安全收尾后释放操作占位；保存 round/reset fact/remaining/attempted，同一事实重试不充值。只有新的可信重置事实允许新有限轮次，同 wake 只消费一次。

### CR10 · P1 · 手动网络失败会留下未来自动执行

位置：[service.ts:1414](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1414)。关联 AGF-D05。

network_wait 分支对所有 trigger 写一分钟后重试并清空候选集合，未排除 manual。用户手动指定 B 失败后，仍可能在一分钟后自行重试该切换，违反手动失败终结规则。

修复：只为有效 workflow 自动需求保存后续唤醒。手动请求在安全回滚后终结并保留失败结果；不要借关联工作流为本次手动意图取得自动授权。

### CR11 · P1 · 停止中重启仍跳过清理，新清理分支也会吞失败

位置：[service.ts:1176](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1176)、[reconcile.ts:24](C:/Code/system-handle/packages/agy-accounts/src/reconcile.ts:24)。关联 AGF-D06。

service 在调用 reconciler 前继续跳过 desired_enabled=false，新 cleanup_only 不可达。该新分支自身又未确认 owned Job/外部进程/实际凭据归属就恢复 backup，吞恢复错误后标 cancelled 并清 pending；不能直接将它接通。

修复：按持久 controller/storage lineage 进入专用 cleanup guard，先确认归属和实际停止，再比较当前项属于原日志后回滚、回读及更新 epoch。失败保留 blocked/journal，不清日志、不恢复业务、不隐式启用管理。

### CR12 · P1 · committed/recovering 重投递缺每次真实身份核验

位置：[reconcile.ts:68](C:/Code/system-handle/packages/agy-accounts/src/reconcile.ts:68)、[service.ts:1342](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1342)、[service.ts:1488](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1488)。关联 AGF-D06。

startup 仅比较可选 target ref，缺 ref 时直接通过并跳过进程确认；后续重试跳过普通 operation guard，仅取数据库 account/epoch 调消费者。首次投递失败后外部换号、失锁或用户停止，旧提交仍可被补投递。

修复：每次投递必须通过独立 commit guard，检查持久 commit account/epoch/ref、控制意图、域锁、真实活动项和旧 Job 状态；不一致冻结。补投递沿用原 commit，不重新切号，也不能用当下任意 realm 值改写提交事实。

### CR13 · P1 · strict 夜间策略未传到首次许可及所有消费者

位置：[service.ts:762](C:/Code/system-handle/packages/agy-accounts/src/service.ts:762)、[ports.ts:133](C:/Code/system-handle/packages/agy-accounts/src/ports.ts:133)、[switch-operation.ts:194](C:/Code/system-handle/packages/agy-accounts/src/switch-operation.ts:194)。关联 AGF-D04/D05。

usage request/occupancy 缺夜间约束；首次准入只传白名单和 reset，混合切换只使用触发操作的 night policy。strict 任务可能使用没有刷新证明的账号，或被 normal 触发者稀释。

修复：逐 source 冻结 night policy，贯穿 request、occupancy、demand、batch 和恢复；每次实际准入以当前时刻执行同一完整可用性检查，不用触发者代表所有消费者。

### CR14 · P1 · 刷新证明没有实际写入链路，旧无来源健康仍可准入

位置：[maintenance.ts:40](C:/Code/system-handle/packages/agy-accounts/src/maintenance.ts:40)、[selector.ts:318](C:/Code/system-handle/packages/agy-accounts/src/selector.ts:318)、[repository.ts:81](C:/Code/system-handle/packages/agy-accounts/src/repository.ts:81)。关联 AGF-D04/D11。

新增 evaluateRefreshObservation 仅为无调用者的纯函数，未形成生产 last_refresh_verified_at 写入；函数也未验证 lease/permit、非交互来源和 capture 成功。selector 只看旧时间戳，忽略 metadata_status，repository 没有无来源旧字段的资格降级。因此新账号无法积累严格资格，旧伪健康记录仍可能通过。

修复：从实际辅助/业务许可积累同身份、自然过期、新 expiry 推进及请求成功事实，capture 成功后持久化证明；先验证归属、代次和协议来源。迁移保留旧历史，但无来源值不得授予 strict 资格；不新增备用账号保活请求。

### CR15 · P2 · 自动唤醒时刻仍可能由白名单外账号决定

位置：[switch-operation.ts:397](C:/Code/system-handle/packages/agy-accounts/src/switch-operation.ts:397)、[wait-policy.ts:29](C:/Code/system-handle/packages/agy-accounts/src/wait-policy.ts:29)。关联 AGF-D05。

computeDomainWait 仍接收全部账号及有限参数，没有统一白名单、night、cooldown/Retry-After 和 capability。只允许 B 两小时后恢复，禁止 C 十分钟后恢复时，C 仍可能决定过早唤醒，触发无意义请求。

修复：等待与选择/准入复用同一冻结约束和可用性评估；单账号所有阻塞取 max，仅合法且条件完整的账号参与跨账号 min，未知时刻不制造自动唤醒。

### CR16 · P2 · 今晚候选报告按当前时间计算，却标为入夜时间

位置：[maintenance.ts:153](C:/Code/system-handle/packages/agy-accounts/src/maintenance.ts:153)、[maintenance.ts:240](C:/Code/system-handle/packages/agy-accounts/src/maintenance.ts:240)。关联 AGF-D04。

报告 evaluation_time 写下一 night_start，selection 及刷新年龄仍使用 now。17:30 时刷新证明还有一小时有效、20:00 已过期的账号，会被错误列入今晚候选；夜间才重置的账号也会被提前排除。

修复：计算统一 `evaluationTime=max(now,night_start_at)`，用于候选、年龄和分组；实际夜间许可仍在当时重新校验。显示时刻必须与计算时刻相同。

### CR17 · P2 · 维护后的原账号不合格时仍直接安排恢复

位置：[service.ts:1815](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1815)、[service.ts:1870](C:/Code/system-handle/packages/agy-accounts/src/service.ts:1870)。关联 AGF-D04/D05。

维护结束无条件 rollback 到 before account，随后投递全部消费者；没有按维护新发现的耗尽、授权失效或夜间资格决定恢复去向。恢复可能立即失败，亦没有进入已有授权的替代 demand。

修复：安全恢复凭据后，逐目标判断原身份资格，合格者才投递；不合格者仅按既有自动授权进入 D05，其余保持明确等待。恢复凭据不等于具备恢复业务资格。

## 根会话、子工作、预算和工作区

### CR18 · P1 · exact_only 仍无法控制实际启动，新 continuation 写读键不一致

位置：[conversation-lineage.ts:78](C:/Code/system-handle/packages/core/src/conversation-lineage.ts:78)、[agy-workflow-recovery.ts:180](C:/Code/system-handle/packages/runtime/src/agy-workflow-recovery.ts:180)、[agy-workflow-bridge.ts:547](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:547)。关联 AGF-D07/D09。

新 coordinator 没有生产调用者；唯一写入按 source Run ID 保存 continuation，启动端却按新 target Run ID 读取。当前仍走旧 stageAccountModelRunRetry，账号 scope 改变后指纹不同，profile-runtime 会 prepare，SDK 随即使用 --new-project。即便目标模型缓存已具备，exact_only 仍可能新建根。新决策器还把“无原 ID”直接当允许 recreate，也不符合原计划。

修复：commit→可信决策器→固定目标 Run→启动必须接通，continuation 绑定目标及源；只允许已授权的身份变化，核验原模型/权限/CWD。无原 ID 或未知结果转 manual；必须有官方确证不可用、旧 Job 全停、材料完整及原策略许可才能 recreate。

### CR19 · P1 · 恢复安排没有原子 intent、固定 target Run 和 outbox

位置：[agy-workflow-bridge.ts:547](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:547)、[agy-workflow-recovery.ts:143](C:/Code/system-handle/packages/runtime/src/agy-workflow-recovery.ts:143)。关联 AGF-D07/D09。

当前 staging、恢复 workflow、删除 wait、异步 dispatch 分开执行；新 progress 仅写 revision=1，不分配 target_run_id/delivery_id。崩溃于安排、Run 创建或 spawn/init 之间时，没有固定恢复目标及投递事实可用于安全补投，仍落入普通重启恢复。

修复：在同步 Store 事务中固定 recovery intent、target Run ID 和 outbox，按 recovery ID/代次去重；投递前 CAS 源/用户控制/计划/策略/账号，已启动结果未知时查 Run/Job/native，保持 delivery_unknown，不增加 generation 重发原 prompt。

### CR20 · P1 · 业务恢复仍获得完整 timeout，新预算计算又包含等待时间

位置：[engine.ts:2889](C:/Code/system-handle/packages/core/src/engine.ts:2889)、[engine.ts:2282](C:/Code/system-handle/packages/core/src/engine.ts:2282)、[agy-workflow-recovery.ts:49](C:/Code/system-handle/packages/runtime/src/agy-workflow-recovery.ts:49)。关联 AGF-D07/D09。

实际新 Run 重新获得完整 agent timeout，aside 重置十分钟。新增 remaining 仅在未调用 coordinator 中计算，采用 commit 时墙钟减 started_at，会把等待账号/认证的时间扣成执行耗时。

修复：在 Job 停止确认时冻结实际已用与剩余执行预算，恢复端只消费 remaining；等待不扣、重复切换不充值。预算为零保持 TIMEOUT，无法确认崩溃期间耗时则按原计划保守处理。

### CR21 · P1 · aside 的账号等待不可恢复，还扩大到普通模型错误

位置：[asides/service.ts:240](C:/Code/system-handle/packages/asides/src/service.ts:240)、[agy-workflow-bridge.ts:535](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:535)。关联 AGF-D07/D09。

任意 MODEL_ACCESS_REQUIRED（包括非 AGY）或匹配文本都能被设 waiting_account 并保留全局提问槽；resumeFromAccountWait 没有调用者，Bridge 又拒绝 aside 触发并在提交回调删其 wait。failSession 也不接收 waiting_account 的最终失败。这可让后续提问长期排队。Bridge 仍兼写/删除 workflow 单键 wait，主 Run 与 aside 并存时也会互相影响。

修复：只有实际、有效账号恢复项才进入 waiting_account；按 source Run/原问题独立恢复、取消和结算，连接 Engine 的启动及结果处理。删除 workflow 单键作为调度依据的用法，兼容历史只做无歧义迁移。最终错误或取消必须释放槽，不能按任意模型错误文本生成永久等待。

### CR22 · P1 · 最终 source checkpoint 与工作区保全没有生产调用

位置：[agy-workflow-bridge.ts:399](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:399)、[agy-workflow-bridge.ts:459](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:459)、[agy-workspace-checkpoint.ts:65](C:/Code/system-handle/packages/runtime/src/agy-workspace-checkpoint.ts:65)。关联 AGF-D07/D08。

prepareSwitch 仍在停止前冻结旧 checkpoint；confirmStopped 只确认 Job，未吸收最终事件、扫描工作区或冻结新的 source。create/restoreWorkspaceCheckpoint 都没有生产调用者。停止中的完成事件和最后写入不能进入恢复材料，临时 worktree 消失后没有可恢复成果。

修复：停止前保存准备状态并持续本地增量保全；Job 全停且事件已收束后最终扫描、冻结 source，账号 commit 后再生成目标 manifest。不完整目标保留具体原因，不授予无人接替资格。

### CR23 · P2 · 子事件代次、游标和工作区被丢弃，终态保护不完整

位置：[agy-workflow-bridge.ts:90](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:90)、[subagent-observer.ts:51](C:/Code/system-handle/packages/adapters/agy/src/subagent-observer.ts:51)。关联 AGF-D08。

Bridge 未保存新增 generation/cursor/workspace/completeness，缺省 parent_id 还能抹掉已有父节点。observer 只按 ID 拦迟到 spawn，旧 cancel/complete 仍能覆盖新结果，合法新代次 spawn 又会被拒绝。

修复：在唯一 agy_subagent 权威记录中持久比较 epoch/generation/cursor，过时事件不覆盖；缺省字段不抹除已有事实。未知格式记录不完整，父子/孙节点、工作区和产物信息进入最终 source。

### CR24 · P2 · 新快照函数漏文件后仍可报告 complete

位置：[agy-workspace-checkpoint.ts:133](C:/Code/system-handle/packages/runtime/src/agy-workspace-checkpoint.ts:133)、[agy-workspace-checkpoint.ts:178](C:/Code/system-handle/packages/runtime/src/agy-workspace-checkpoint.ts:178)。关联 AGF-D08。

尚未接通的新函数使用非 -z porcelain，只去首尾引号，无法还原中文/转义文件名；不存在的路径直接跳过。非 Git 目录没有采集内容也会 complete=true，tracked 只保存 HEAD 合并 diff，未保存 index/worktree 两层。接入前必须修正这些数据丢失路径。

修复：用 NUL 分隔、确定仓库根的路径采集，缺失/无法解释项明确列未保全；非 Git 显式不支持或实际枚举。分开保存 index/worktree，完整校验后原子发布快照，不截断后报告完成。

### CR25 · P1 · 新恢复函数会直接覆盖文件，未验证范围和内容

位置：[agy-workspace-checkpoint.ts:148](C:/Code/system-handle/packages/runtime/src/agy-workspace-checkpoint.ts:148)、[agy-workspace-checkpoint.ts:227](C:/Code/system-handle/packages/runtime/src/agy-workspace-checkpoint.ts:227)。关联 AGF-D08。

该函数目前无生产调用者，但直接 resolve manifest 路径后 writeFileSync，未验证批准目录、越界、现有修改、base 或 artifact 摘要；采集时 stat/read 会跟随链接。缺 diff artifact 还可返回 success；中途 apply 失败前已覆盖部分 untracked。

修复：先完整验证 manifest/base/所有摘要/路径及 reparse/link，越界或缺材料直接失败；原路径有新修改转冲突，只在已有授权隔离目录恢复。完成预检后再写入，不允许部分覆盖用户文件后才报告错误。

## 模型验证、API 与界面接线

### CR26 · P1 · 候选模型访问缓存未接通，新方法会直接伪造 verified

位置：[model-access-service.ts:673](C:/Code/system-handle/packages/core/src/model-access-service.ts:673)、[model-identity.ts:147](C:/Code/system-handle/packages/core/src/model-identity.ts:147)、[agy-workflow-bridge.ts:547](C:/Code/system-handle/packages/runtime/src/agy-workflow-bridge.ts:547)。关联 AGF-D09。

verifyFrozenForAccountRecovery 没有生产调用者，现有候选 probe 的布尔缓存不能满足 ModelAccessService 的精确访问缓存；B 尚未验证过原冻结模型时，账号提交后仍会阻塞 MODEL_ACCESS_REQUIRED。新方法本身也没有请求 CLI/校验 lease，直接写 status=verified、verification_method=native-probe；候选 identity 交给 resolveNativeConfig 后，在 managed 模式又被旧 committed realm A 覆盖。不能通过简单调用这个方法解决缺口。

修复：在当前协调操作拥有的辅助许可内验证原 FrozenInvocation，候选身份来自已核实 lease/credential/epoch，显式绕开读取旧 committed 身份的逻辑但不扩大 HTTP 权限。开始/结束都校验，真实成功后写 B 的既有缓存键；不重入 coordinator、不改变原模型/effort/订阅来源。

### CR27 · P2 · 恢复列表查询错误的存储键，正常数据始终查不到

位置：[agy-account-routes.ts:348](C:/Code/system-handle/apps/api/src/agy-account-routes.ts:348)、[agy-recovery-checkpoint.ts:106](C:/Code/system-handle/packages/runtime/src/agy-recovery-checkpoint.ts:106)。关联 AGF-D10。

API 用 getRecord("agy_recovery_progress", "default") 读取数组；实际每条 progress 按 recovery_id 保存、owner=operation_id，没有该数组。即使产生了 progress，列表也为空；progress 没有 workflow_id，`|| !p.workflow_id` 不能正确过滤归属。该工作流路由还注册在 accounts/full 共用账号路由中。

修复：在 full 工作流路由提供按 workflow/source 索引的列表，通过 source/manifest 核验归属并生成安全 DTO；不要从 default 键猜数组，也不要将工作流路由暴露给 accounts 模式。

### CR28 · P1 · 取消恢复接口只改显示状态，实际恢复仍可能继续

位置：[agy-account-routes.ts:357](C:/Code/system-handle/apps/api/src/agy-account-routes.ts:357)。关联 AGF-D09/D10。

接口不使用 URL 的 workflowId 验证归属，也不检查 request_id/revision；只改 progress.state，目标不存在也回 cancelled=true。实际 Bridge/Engine 恢复不读取这个状态，因此取消不会撤销待投递或已运行目标，存在“界面取消成功、后台继续执行”的风险。

修复：在 full 模式调用唯一恢复协调器，以请求去重和 revision/control CAS 撤销对应 intent/outbox；已启动时走明确目标的现有停止链。验证 workflow 归属，缺目标返回明确错误，不影响其他恢复项；完成实际状态变更后返回相应取消进度。

### CR29 · P2 · 逐能力、FIFO 和逐目标恢复展示没有接入页面

位置：[agy-account-routes.ts:339](C:/Code/system-handle/apps/api/src/agy-account-routes.ts:339)、[agy-accounts.ts:97](C:/Code/system-handle/packages/presentation/src/agy-accounts.ts:97)、[AgyAccountsPanel.tsx:44](C:/Code/system-handle/apps/web/src/components/AgyAccountsPanel.tsx:44)。关联 AGF-D10。

新增 capabilities 路由只包装原 capability.supported 总布尔；新能力对象、恢复/健康格式化函数没有调用者。账号页仍读取旧 DTO，没有 FIFO/batch、逐恢复目标、未知投递及剩余预算展示；维护页也未接新增评价时刻与未知 expiry 分组。已有双额度/基础认证展示保留，但不能代替 D10 新增内容。

修复：核心和恢复层提供单一安全 DTO，分别接账号 Panel、Maintenance、工作流运行展示；每个目标区分已切号、已安排、已观察运行及需处理。GET/倒计时只读本地，独立页继续零工作流依赖，不新增另一套前端选号算法。

## 执行交接和复审边界

继续以原 AGF-D00–D12 及原 AC-R/D/U/I/E/L/H 为依据，本文 CR01–CR29 是缺陷定位和关闭条件；执行者不得另写替代计划，也不能只让新纯函数测试通过而保留旧生产调用链。

- 认证组负责 CR01–CR07，原生 Host 和 probe/login/enrollment 文件归该组；与核心冻结安全元数据及辅助 lease 接口。
- 核心组负责 CR08–CR17，独占 service/switch/selector/wait/reconcile/maintenance/repository；先修统一评估，再接实际消费者与持久状态。
- 恢复组负责 CR18–CR25，检查点、子观察和工作区文件可独立推进；Engine/Bridge/model-retry/conversation-lineage 等共享文件由指定整合负责人独占编辑。
- 整合负责人完成 CR26 及共享 contracts/ports/组合根；API/界面组负责 CR27–CR29，取消必须接真实恢复协调器，不能仅写状态。

互不依赖的开发和测试代码并行；同一共享文件采用明确交接。正式范围开发、异常处理和接线完成后，由测试 Agent 并行执行各自独立目标，每条命令一个文件/类/用例，分别修复并定向重跑，不要求无筛选全量测试。资源使用各自临时数据库、端口、输出及假认证域，不操作用户当前凭据或服务。随后按认证/核心/恢复/API 边界只读并行复审，主审汇总跨模块交互，不重复审计测试附件。

涉及官方真实能力的受阻项保持明确状态；本轮没有替用户批准真实账号采集、切号或模型请求。源码整改与模拟测试完成，不自动变成实机或人工验收通过。后续提交只包含明确白名单，保留其他已有代码和计划；本文审查不执行提交、推送、发布或生产重启。
