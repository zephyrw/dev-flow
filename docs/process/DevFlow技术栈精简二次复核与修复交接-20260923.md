# DevFlow 技术栈精简二次复核与修复交接

日期：2026-09-23

## 结论与范围

对整改提交 `0aa999d0dac4aaa538bfba10b0da471382c09390` 复核后，仍发现会导致任务无法启动、账号功能不可用、停止状态误判、凭据恢复不可靠及升级误判的问题。本轮已直接修改实现，不只是提出修复建议。

- 工作目录：`C:\Code\system-handle\.claude\worktrees\devflow-node-migration`
- 分支：`worktree-devflow-node-migration`
- 修改以未提交 diff 留在该 worktree，未提交、未合并、未发布。
- 主工作区已有改动、当前数据库、凭据、账号会话与运行服务未被本轮操作。
- 本轮不执行测试。测试夹具已调整，但这些测试尚未执行，不能视为功能验收通过。
- 延续 Node/TypeScript + 既有 koffi 实现，没有增加新的运行时、语言项目或业务阶段。

## 已修复的问题

| 编号 | 问题及影响 | 修复后的行为 | 主要代码 |
|---|---|---|---|
| F01 | manager 启动 runner 后未建立完整 ready/start 协议，ESM 使用 require，执行可能一直等待 | 统一使用编译后的 runner-entry.js；核对 attempt/version/pid，绑定 Job/进程组并登记后才发送 start；接收实际工具 PID，支持受控 stdin | packages/process/src/manager.ts、runner-entry.ts |
| F02 | 只确认根 PID 消失就当作整树停止；重试可能读取上次停止结果 | 每次启动独立 attempt，当前实例优先；Windows 查询 Job 活跃数，POSIX 查询进程组并核对启动身份；排空输出后完成；查询失败保留 unknown 并返回 PROCESS_STOP_UNCONFIRMED | manager.ts、process-protocol.ts、native/windows.ts、native/posix.ts |
| F03 | 账号停止检查遇到记录缺失、PID 复用或归属不明时可能放行 | 工作流恢复与账号检查共用停止观察；缺记录/归属不明阻塞释放；外部 AGY 识别按真实 Job 成员关系排除自有进程 | packages/runtime/src/recovery.ts、packages/process/src/agy-account-processes.ts |
| F04 | 控制器锁名不兼容、同进程递归获取及 POSIX 假锁 | 创建并规范化存储目录；兼容旧锁名并保持固定获取顺序；本进程去重；POSIX 使用真实 flock，释放不删锁文件 | controller-lock.ts、native/posix.ts |
| F05 | 登录与辅助探测没有统一进程归属、超时与停止确认 | 使用同一个 ProcessManager 实现；Windows 交互登录先挂起创建、绑定 Job，再恢复；取消、输出上限与临时目录清理均依赖停止确认；辅助进程记录持久化；启动版本查询也受管 | agy-account-job-runner.ts、native/windows.ts、apps/api/src/account-service-bootstrap.ts |
| F06 | 凭据锁实现返回空值，锁名截断，与旧认证域不互斥 | worker 内持有真实 Windows Mutex，使用 SID 与固定凭据目标的完整 SHA-256；RPC 串行化；除能力检查与取锁外，每项操作验证 realm + lock_id | packages/agy-accounts/src/credential-worker.ts、credential-windows.ts |
| F07 | Vault 格式、加密层次、备份命名与恢复读取不兼容；失败被当作不存在 | 还原 sec_/bak_ 引用、.bin 文件及 Version/RealmID/AccountID/Revision/Credential v2 envelope；整体 DPAPI 加密；按指定引用加载；完整保存 Credential 属性；备份和写入读回校验；单调 revision、原子写入、ACL 与 reparse 检查 | credential-store.ts、credential-windows.ts、credential-worker.ts |
| F08 | 主进程直接探测凭据原生 API；worker 路径和启动方式不受约束；断线与并发 release 可留下 pending 请求 | 主进程只接收白名单元数据；固定编译入口由受管 runner 启动；generation 隔离、严格响应校验、消息上限、超时失锁和整树停止；release 清理并发请求；未确认退出不得创建替代 owner | auth-host.ts、credential-worker.ts、manager.ts |
| F09 | 服务仍检查旧 2.x 版本，拒绝新 worker；仅因平台是 Windows 就显示能力可用 | 新协议明确为 3.0.0-node；未探测字段保持 unverified/false；由 worker 实际探测原生能力，不把文件存在当成功 | packages/agy-accounts/src/service.ts、auth-host.ts、account-service-bootstrap.ts |
| F10 | 运行配置残留 host；迁移器重复、会丢注释、可能删除自定义设置 | 单一 v1→v2 规则用于 loader、CLI、setup、installer；只迁移约定的内置路径，拒绝自定义路径和未知字段；YAML 节点编辑保留其他注释；apply 校验 hash、备份原字节、临时文件同步后替换；移除设置模型中的旧可执行路径 | packages/contracts/src/config.ts、config-migration.ts、scripts/migrate-node-runtime.ts、scripts/setup.mjs |
| F11 | 安装器未保护旧进程与数据，可能把旧服务健康当成新安装成功 | 升级前取得兼容控制器锁，检查进程和租约，备份 SQLite/指针；校验安装后的原生依赖；先选定版本再启动；健康返回并核对实际安装目录；写入新版本状态后不自动覆盖数据库回滚 | packages/installer/src/main.ts、upgrade.ts、packages/service/src/launcher.ts、apps/api/src/base-server.ts |
| F12 | 构建、CI、发布和基准脚本仍引用已删除 Go/.NET Host；源码与编译 runner 两份实现并存 | 删除旧 builder、脚本入口、组件声明、CI Go 步骤及手写 runner-entry.js；发布验证 Node 子进程入口与打包后的原生依赖；测试前先构建 | package.json、components.lock.json、.github/workflows、scripts/release、scripts/bench、README.md |

额外收敛：CLI init 只生成 v2 配置；CLI recover/retry-commit 先取得控制器锁，避免与正在运行的控制器同时修改恢复状态。源码开发也必须先构建受管子进程入口，不再维护第二套手写 JS。

## 静态检查记录

- 已通过生产源码 `node node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit --pretty false`。
- 已通过全项目 `node node_modules/typescript/bin/tsc --noEmit --pretty false`，覆盖源码与测试的类型检查。
- 全项目检查最初发现 10 处旧测试接口错误；已改为新契约并保留异常场景断言。新增/调整了 managed channel 断线、旧 generation、释放锁时并发请求的单元测试夹具。
- `git diff --check` 已通过。
- setup、release builder、benchmark 的 `node --check` 语法检查已通过；没有执行这些脚本。
- 本轮未执行 Vitest、Playwright、复现程序、基准测试、安装器、服务启动、真实登录或凭据读写。构建后的产物也未作为本轮运行验证证据。
- 静态检查通过只表示类型与文本检查通过，不代表 Job、Mutex、DPAPI、跨平台进程树或真实账号切换已经验收。

## 另一位 Agent 的执行顺序

在上述 worktree 内继续，保留现有未提交修改。不要通过恢复旧 Host、放宽 unknown 判定、删除异常断言或重新允许 worker 路径注入来使测试通过。

1. 先记录 HEAD 与 diff，确认接收的是本轮工作区。
2. 先运行 `pnpm build`，生成唯一的 runner 与 credential worker 编译入口。不要使用工作区原有 dist 作为新代码证据。
3. 运行 `pnpm typecheck`。
4. 先执行下列定向回归，再运行完整单元/集成测试与相关浏览器测试。出现失败先区分旧夹具契约、真实实现缺陷与机器环境问题。
5. 在测试报告中记录具体命令、平台、实际结果和未覆盖部分；不要将本轮“未运行”写为通过。

建议首轮：

```powershell
pnpm exec vitest run tests/unit/process-protocol.test.ts tests/unit/agy-account-processes.test.ts tests/unit/agy-auth-host-pipes.test.ts tests/unit/agy-account-config.test.ts tests/unit/agy-account-service.test.ts tests/unit/agy-account-release.test.ts --maxWorkers=1
pnpm exec vitest run tests/integration/node-native-primitives.test.ts tests/integration/process.test.ts tests/integration/recovery.test.ts tests/integration/agy-account-install.test.ts tests/integration/agy-account-safety.test.ts tests/integration/agy-account-switch.test.ts tests/integration/preparation-failure.test.ts tests/integration/check-repair.test.ts tests/integration/precommit-runtime.test.ts tests/integration/devflow-v2-installer.test.ts --maxWorkers=1
```

现有原生测试还需核对是否沿用旧 runner 协议、旧返回值或直接 PID 断言；本轮没有运行这些测试，不能保证只改上述夹具就足够。

## 必须实际覆盖的回归清单

| 范围 | 必测场景与通过条件 |
|---|---|
| 启动 | ready→绑定/登记→start 顺序；错误版本、错误 attempt、Job 绑定失败、构建入口缺失均不得执行工具；普通 JS/CLI 确实运行并返回真实工具 PID |
| 输出与输入 | 二进制 stdout/stderr、UTF-8 分块、较大输出、stdin 关闭/EPIPE；最终输出排空；原始输出不混入 IPC；credential worker 的 stderr 不进入业务日志 |
| 生命周期 | 根进程先退出但子孙存活、手动停止、超时、强杀控制器、启动中取消、重复 stop、同 id 再次 start；旧回调不能覆盖新 attempt；未知状态不能释放租约 |
| Windows | 实际 Job 成员关系、查询失败、访问拒绝、PID 复用；交互登录先挂起绑定再恢复；取消后整个 Job 为空 |
| POSIX | Linux/macOS 真实 flock 互斥及崩溃释放；进程组 TERM/KILL 升级；子孙尚存时不得认为完成；确认平台差异，不能用 Windows 结果替代 |
| 控制器锁 | 同进程重复获取、不同进程争用、旧/新锁名互斥、路径大小写与目录别名、未创建存储目录、异常中途取锁的逆序释放 |
| AuthHost | 断线时立即失锁、全部 pending 结束、旧 generation 事件无效；release 与 capability/inspect 并发不挂起；停止未知时不能重启替代 worker；响应超限、畸形 JSON、未知字段拒绝 |
| 凭据兼容 | 在隔离 Windows 用户/虚拟机中读取旧 v2 DPAPI fixture；完整 Credential 属性往返；空凭据 capture/clear/restore；多个备份按给定 ref 精确恢复；损坏/不存在/ACL/reparse 错误不得当成功 |
| 凭据一致性 | 真实域互斥；无锁与跨 realm 操作拒绝；保存后读回失败不清除目标；写入失败不更新成功状态；revision 单调；删除活动引用拒绝 |
| 配置 | v1→v2、已是 v2 的幂等性、内置相对/Windows 绝对路径、原 CLI 的 .NET 默认路径、自定义路径、伪装前后缀、未知字段；业务设置和注释保留；dry-run/apply 哈希一致；并发改文件拒绝 |
| 升级与发布 | 旧控制器存在/未知进程/活动租约拒绝升级；一致性 SQLite 备份；安装包缺入口或原生 addon 时失败；旧安装目录健康不能冒充新版本；新状态写入后的失败保留诊断，不自动倒灌数据库 |
| 净环境 | 仅 Node/pnpm，无 Go、dotnet SDK 和 dist/host，也能构建及加载发布包；不要把“编译成功”替代 Windows 凭据能力、官方双额度、A→B→A 等真实验收 |

凭据测试必须使用隔离测试账号或虚拟机与合成数据；不要覆盖开发者当前 Windows 用户的 `gemini:antigravity` 凭据。真实业务账号能力仍按原验收边界单独验证。

## 失败处置边界

- 停止未确认时保留进程记录与相关临时目录，不清凭据、不释放资源。
- 旧记录没有可验证身份时保持阻塞，不能补写虚假的 confirmed 标记。
- 自定义旧 Host 配置保持原文件，由使用者确认如何迁移，不静默替换。
- 安装阶段在新版本开始写入状态前可恢复配置/指针；之后保留新状态与备份，分析实际写入后再决定恢复步骤。
- 当前改动尚未通过运行测试，测试完成前不得声称整改已获功能验收。
