# 模型配置复核修复执行记录

原任务日期：2026-09-20；最终验证日期：2026-09-21。

依据：[原详细实施计划](../plan/DevFlow模型配置与运行中切换详细实施计划-20260918.md)、[工具模型清单](../plan/DevFlow工具模型与思考强度核对清单-20260918.md)及[初次代码质量复核意见](../plan/DevFlow模型配置与运行中切换代码质量复核与整改意见-20260920.md)。本文件仅记录实际执行，不替代原计划，不增加工作流阶段或材料门槛。

## 复核结果

代码质量复核通过，已修复复核确认的问题，并保留主分支的轻量调度和账号管理功能。用户已授权直接修复、提交、合并及清理目标 worktree。

已处理的主要问题：

- 模型目录发现、刷新、目录外模型输入和工具身份校验；验证与实际派发使用同一可执行文件和精确模型、思考强度参数。
- Run 保存完整冻结调用和 model_binding；普通配置更新只影响后续派发，自动重试沿用原轮模型、角色、修复批次与会话归属。
- 暂停恢复保留原阶段、计划、快照和审查上下文；跨工具、模型或思考强度变化不误用旧会话。
- 模型访问只在真实受控探针终态成功后缓存；按提供方及稳定账号身份隔离，明确登录失效或无权限时失效，强制重新验证、取消和长验证正确处理。
- 人工复测的修复模型选择、批次指派及清除使用事务、版本和幂等收据；保留未修改的指派，任务默认值与本批次覆盖分离。
- 全局默认设置、创建任务、任务中切换、质量修复及人工问题处理入口；异步刷新保留草稿，冲突和错误可见；安装覆盖不重置未指定字段，未验证配置仅保存为草稿。
- 旧任务保持 revision 0 只读兼容视图和原业务协议；无快照轻量人工确认及提交后确认状态展示正确，原确认凭据不被改写。
- 与 main/b60d9dc 整合后，账号恢复只更新授权身份，保留原轮模型和思考强度。新账号未验证时保持现场，不触发代码整改；取得执行许可后再核对身份，变化时释放许可。
- HTTP/MCP 共用模型验证服务；受管验证与账号切换使用同一协调队列，前后检查账号、代次和外部凭证变化。关闭服务时先取消模型验证，再排空账号队列。原账号能力门禁及 HTTP 隔离保持。

## 验证结果

首轮定向单元及集成验证：26 个文件，335 个用例通过。具体包含适配器及授权 146、默认值与安装 28、API 43、运行与入口 118 个用例。

整合最新主分支后的定向验证：16 个文件，154 个用例通过：

| 分组 | 文件及通过用例数 | 合计 |
| --- | --- | --- |
| 运行恢复 | agy-account-model-recovery 11；agy-account-workflow-bridge 6；model-runtime-boundaries 12；model-retry 4；review-pause-resume 8 | 41 |
| 授权与安装 | model-access 45；devflow-v2-installer 22；agy-account-install 3；agy-account-service-mode 3 | 73 |
| API 与交接 | base-server 3；agy-model-verification 6；agy-account-repair 5；agy-accounts-standalone 6；model-confirmation-api 16；devflow-v2-mcp 1；runtime-lightweight-fixes 3 | 40 |

两个阶段有重复，不将 335 与 154 直接相加。每条测试命令只运行一个文件，使用单 worker。初次并发造成的本机资源超时在独占串行验证中消除，未扩大产品或测试超时。

浏览器最终验证：model-settings 15、model-switch 5、repair-model-picker 8，共 28 个用例全部通过。包括默认设置、新建任务预填、实际模型参数、授权重用、长验证、错误重试、保留草稿、双窗口冲突、两个审查阶段暂停恢复及人工修复处理者修改。使用专用端口 24831，本次测试服务和子进程均已退出。

`pnpm exec tsc --noEmit` 与 `pnpm run build` 通过。构建仅有既有依赖注释及大体积 chunk 提示，无构建错误。

验证中同步修复的整合问题与夹具问题：MCP 公共服务器恢复原动态端口配置读取；测试夹具使用合法提交 hash 和 providerScope 默认值；目录刷新返回匹配 AGY 身份的受控输出并等待真实完成；原生功能反馈用合法 native-v2 任务，不改变旧协议分流。浏览器断言未以允许失败、提前返回或跳过方式放宽。

## Git 交付

- 实现与初步修复快照：97dfa32。
- 整合 main/e8c508f 及轻量调度复核修复：77f63a3。
- 最新 main/b60d9dc 已在目标 worktree 整合并完成上述验证，形成提交 63b5d11（保持模型切换与账号恢复的一致性）。
- 本地 main 已快进合并至 63b5d11；目标 worktree `C:/Code/system-handle-model-config` 和临时分支 `feat/model-config-runtime-switch-20260918` 已删除，其余 6 个工作区保留。
- 清理时停止了经进程身份和加载模块路径确认属于目标 worktree 的旧测试夹具进程，以释放 SQLite 二进制文件占用；未停止正式 API 服务。
- 主工作区 `.tmp-wf-query.cjs` 的原 AD 状态及暂存 blob 保持不变；两份无关未跟踪计划的哈希保持不变。三份相关重名文档的原件及核验清单保留在 `.cache/model-config-merge-preserved-20260921-000024`。

本次未调用真实模型、未更改真实账号授权，未部署或重启正式服务。真实工具连通和功能效果仍需在实际使用中确认，夹具验证不替代人工验收。
