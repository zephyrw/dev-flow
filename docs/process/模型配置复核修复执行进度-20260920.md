# 模型配置复核修复执行进度（非计划）

依据：原详细实施计划、配套模型清单及 20260920 代码质量复核意见。

本记录只记录实际执行，不替代原计划。用户已授权复核、直接修复、提交合并及清理目标 worktree。

- 已确认目标：feat/model-config-runtime-switch-20260918，基础 df18ee4。
- 已完成最新实现的分模块只读复核；上一轮问题多数已有修复，剩余问题按原编号修正。
- 主 main 现有轻量调度提交 e8c508f 必须保留；主工作区其他账号管理改动保持独立。
- 已修复调用参数冻结、模型/账号作用域、授权终态和缓存失效、暂停与会话归属、修复指派事务及版本、UI 异步操作和入口问题。

- 已保存实现与整改快照：97dfa32。正在独立 worktree 中整合 main/e8c508f；原生调用、授权缓存、修复指派与异步入口的修复已落地。
- 合并保留 main 的轻量完成记录、可选附件、等待来源和质量计数；工具启动方式不得恢复旧业务证明门槛。
- 已解决与 main/e8c508f 的所有合并冲突，代码差异检查通过；仍在目标 worktree 中，主工作区文件未写入。

## 当前验证记录

- `pnpm exec tsc --noEmit`：通过。
- `pnpm run build`：通过；存在依赖注释和大体积 chunk 提示，无构建错误。
- 适配器及授权 9 个独立文件共 146 个用例通过：adapter-executable-resolver 3、model-catalog 8、model-catalog-adapters 12、model-catalog-service 13、model-selection 31、frozen-invocation 8、native-model-identity 5、probe-terminal 28、model-access 38。
- model-defaults：6 个通过；devflow-v2-installer：22 个通过。
- model-api：8 个通过；model-configuration-api：19 个通过；model-confirmation-api：16 个通过。
- 运行与入口 12 个独立文件共 118 个用例通过：model-runtime-boundaries 12、model-switch 23、repair-model-assignment 21、legacy-model-configuration 5、model-retry 4、run-telemetry 1、review-pause-resume 8、devflow-v2-mcp 1、devflow-v2-create 3、devflow-v2-feedback 3、model-routing 19、devflow-v2-quality 18。
- 上述单元及集成用例合计 335 个（重复运行不重复计数）。
- 初次并发时的目录与 Git 夹具超时已在独占串行验证中消除；未扩大产品或测试超时。
- 新增确认事务夹具的非法假提交 hash 已修正；运行边界用例已按主分支故障分类改用 BUILD_FAILED 验证自动整改，不更改基础设施故障保留现场的行为。
- 浏览器夹具按真实身份作用域创建目录，CLI 强制映射至本地探针；保存、重试及修复处理者用例已去除允许失败的宽松断言。
- 回归进一步修复了 Run 的 model_binding 漏写、无快照的轻量确认接口、提交阶段生成快照后人工确认显示失真。冻结调用与展示绑定共用同一快照；人工确认原 snapshot/proof 保留，只增加受版本和原绑定检查保护的提交快照显示关联。独立交叉复核、完整创建至提交链路、最新类型检查及构建均通过。
- 浏览器回归尚未执行，等待整合最新主分支后运行。

## 合并与运行边界

- 账号管理 Agent 已结束并提交 main/b60d9dc；已只读核验任务完成状态与主工作区状态。接下来先在当前 worktree 整合该提交，再验证功能交界。主工作区原暂存临时文件及其他未跟踪文档继续保留。
- 尚未合并至主工作区，未删除目标 worktree 或临时分支。
- 本次未调用真实模型、未更改真实账号授权、未部署或重启正式服务；真实工具连通和功能验收不能由夹具测试替代。
